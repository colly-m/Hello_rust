#![allow(unused)]

fn main() {
    // Macros in rust generate code at compile time and are invoked with a bang (!)
    // println! is a macro that prints text to the console
    println!("Hello, world!");
}